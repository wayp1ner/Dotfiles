import { JSX } from "react";
interface Props {
    columns: number;
    gap?: string;
    inline?: boolean;
}
export declare function Grid(props: Props & JSX.IntrinsicElements["div"]): JSX.Element;
export {};
